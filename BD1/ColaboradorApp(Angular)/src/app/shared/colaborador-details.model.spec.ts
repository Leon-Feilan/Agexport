import { ColaboradorDetails } from './colaborador-details.model';

describe('ColaboradorDetails', () => {
  it('should create an instance', () => {
    expect(new ColaboradorDetails()).toBeTruthy();
  });
});
