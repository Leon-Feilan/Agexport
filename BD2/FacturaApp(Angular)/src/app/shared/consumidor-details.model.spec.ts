import { ConsumidorDetails } from './consumidor-details.model';

describe('ConsumidorDetails', () => {
  it('should create an instance', () => {
    expect(new ConsumidorDetails()).toBeTruthy();
  });
});
